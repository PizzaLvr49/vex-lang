[package]
name = "vixen"
version = "0.0.0"
edition = "2024"
description = "Reserve for the Vixen VM"
license = "MIT OR Apache-2.0"

[dependencies]
anyhow = { workspace = true }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = true
panic = "abort"
